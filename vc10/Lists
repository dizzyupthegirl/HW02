#include "Circle.h"
#include "Sqaures.h"


class Lists{
public:
    Circle* circ_sentinel;
	Squares* sqr_sentinel;
 
    void reverse();
    Node* insertAfter(Circle* prevNode, int pos_X, int pos_Y);
	Node* insertAfter(Squares* prevNode, int pos_X, int pos_Y);

    Node* onThisRing(int mouseX, int mouseY);
    void resize();
};